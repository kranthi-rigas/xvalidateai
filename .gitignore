.idea
build
package-lock.json
node_modules
.env.development
.DS_Store
.env 
.env.development.example
dist

node_modules/

# Source document folders (originals not needed by the app).
# Anchored to the repo root: unanchored, "aiLiteracy/" also matched
# src/pages/ailiteracy/ on case-insensitive filesystems, so new components
# added there were silently ignored and never committed.
/aiLiteracy/
/ilovepdf_converted/
